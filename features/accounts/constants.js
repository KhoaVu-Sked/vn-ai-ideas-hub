// Seniority level values — shared by the server query layer and client UI.
// Keep in sync with the check constraint in migrations/021_fix_position_values.sql.
export const POSITIONS = ["intern", "junior", "middle", "senior", "principal"];
